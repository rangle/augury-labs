{
  "$schema": "http://json-schema.org/schema",
  "id": "augury-schematic-ng-add",
  "title": "Augury ng-add schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "x-prompt": "What's the name of the application?"
    }
  },
  "required": ["project"],
  "additionalProperties": false
}
